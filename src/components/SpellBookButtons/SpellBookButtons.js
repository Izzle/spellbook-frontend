import React, { Component} from 'react';
import './SpellBookButtons.css';

export default class SpellBookButtons extends Component {

    render() {
        return(<div>
            <button>edit stuff</button>
            <button>make this a Link back to the decks of cards!</button>
        </div>);
    }
}